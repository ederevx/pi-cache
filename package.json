{
  "name": "pi-cache",
  "version": "0.5.1",
  "description": "Prompt-cache-aware pi coding-agent extension: cache telemetry, stable prefix normalization, tool-schema hygiene, and cache-aware fast compaction.",
  "type": "module",
  "license": "MIT",
  "author": "Edrick Sinsuan <evcsinsuan@gmail.com>",
  "homepage": "https://github.com/ederevx/pi-cache",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ederevx/pi-cache.git"
  },
  "keywords": [
    "pi-package",
    "pi",
    "prompt-cache",
    "prompt-caching",
    "compaction"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": {
      "optional": true
    },
    "@earendil-works/pi-coding-agent": {
      "optional": true
    },
    "@earendil-works/pi-tui": {
      "optional": true
    }
  },
  "scripts": {
    "test": "node --experimental-strip-types --experimental-transform-types tests/run.ts"
  }
}